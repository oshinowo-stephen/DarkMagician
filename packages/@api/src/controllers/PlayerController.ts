import {
  Controller,
  Middleware,
  Get, Post, Delete
} from '@overnightjs/core'

import {

} from 'express'

@Controller('player')
export class PlayerController {
    @Post('')
    public async createPlayer (req)
}
