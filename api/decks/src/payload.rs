use serde::{Deserialize, Serialize};

#[derive(Debug, Deserialize, Serialize)]
pub struct Payload {
    pub deck_name: String,
    pub deck_about: String,
    pub deck_cards: 
}

#[derive(Debug, Deserialize, Serialize)]
pub struct Deck
